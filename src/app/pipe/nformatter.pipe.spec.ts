import { NformatterPipe } from './nformatter.pipe';

describe('NformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new NformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
